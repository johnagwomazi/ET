import DashboardPlaceholderPage from "./DashboardPlaceholderPage";

function RoleDashboardPage({ title, description }) {
  return <DashboardPlaceholderPage title={title} description={description} />;
}

export default RoleDashboardPage;
